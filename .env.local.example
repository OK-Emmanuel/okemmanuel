# Sanity project settings (public — safe for client bundle)
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2026-01-01

# Server-only token (write/preview access) — never prefix with NEXT_PUBLIC_
SANITY_API_TOKEN=